---
title: 'Another Test Post'
date: '2023-04-19'
tags: ['next.js', 'nextjs', 'new']
---

We recoomend using **Static Generation** (with and without data) 
whenever possible because your page can built once and served 
by CDN, which males it mauch faster than having a server render the 
page on every reguest.

```ts
const sum = (num1: number, num2: number) => {
    return num1 + num2;
}
console.log (sum(4,8)) //12

Yu can use Static Generation for many taypes of pages, including